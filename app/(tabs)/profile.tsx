import React from 'react';
import { SafeAreaView, StyleSheet, Text } from 'react-native';
export default function Profile(){ return <SafeAreaView style={s.c}><Text style={s.h}>프로필 (추후 추가)</Text></SafeAreaView> }
const s=StyleSheet.create({c:{flex:1,justifyContent:'center',alignItems:'center'},h:{fontSize:20,fontWeight:'600'}});
